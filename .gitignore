node_modules/
.env
.env.local
.next/
out/
.DS_Store
Thumbs.db

.vercel

# Personal broker data - watchlist and holdings. Never commit. The glob is
# deliberately wider than the two files that exist today: the cost of one more
# ignored file is nothing, the cost of one leaked one is not recoverable.
*.local.txt
